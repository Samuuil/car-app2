package com.carapp.backend.model.enums

enum class EngineType {
    diesel,
    electric,
    `electric (fuel cell)`,
    `flex-fuel (FFV)`,
    gas,
    hybrid,
    `mild hybrid`,
    `natural gas (CNG)`,
    `plug-in hybrid`
}
